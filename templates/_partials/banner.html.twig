<!-- Page Banner Section Start -->
<section class="page_banner bg_{{ random(11, 27) }} mb-2">
	<div class="container">
		<div class="row">
			<div class="col-lg-12">
				<div class="pb_content text-center">
					<h2>{{ name }}</h2>
					<p>
						<a href="{{ path('app_home') }}">{% trans %}Accueil{% endtrans %}</a>
						<b>-</b>
						<span>{{ name }}</span>
					</p>
				</div>
			</div>
		</div>
	</div>
</section>
<!-- Page Banner Section End -->
