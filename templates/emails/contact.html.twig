<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Message de contact</title>
		<style type="text/css">
			body {
				font-family: Arial, sans-serif;
				background-color: #f6f6f6;
				padding: 20px;
			}

			.container {
				background-color: #fff;
				border-radius: 5px;
				box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
				padding: 20px;
			}

			.label {
				font-weight: bold;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<h1>Message de contact</h1>
			<hr>
			<p>
				<span class="label">Nom et prénom du client :</span>
				{{ form.get('lastname').getData() }}
				{{ form.get('firstname').getData() }}
			</p>
			<p>
				<span class="label">Adresse e-mail du client :</span>
				{{ form.get('email').getData() }}
			</p>
			<p>
				<span class="label">Téléphone du client :</span>
				{{ form.get('phone').getData() }}
			</p>
			<hr>
			<p>
				<span class="label">Objet du message :</span>
				{{ form.get('objet').getData() }}
			</p>
			<p>
				<span class="label">Message :</span>
				{{ form.get('message').getData() | raw }}
			</p>
		</div>
	</body>
</html>
